[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
# `requires_cobc` is applied via conftest.requires_cobc (a skipif), not a raw
# marker, so no custom marker registration is needed. This file just pins the
# test layout so `pytest` works from the repo root with zero arguments.
