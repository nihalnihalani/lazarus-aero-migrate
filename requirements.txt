google-genai>=1.55.0  # Interactions API / Managed Agents require >=1.55.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sse-starlette>=2.0.0
pytest>=8.0.0
# System dependency (installed in the sandbox, not via pip): gnucobol  ->  apt-get install -y gnucobol
